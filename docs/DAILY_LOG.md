# Film Recipe 每日工作记录

## 2026-08-17

### 今日完成

- 排查 `src/main.tsx` 中 `@/styles.css` 的编辑器模块解析报错。
- 将入口全局样式改为相对导入 `./styles.css`。
- 新增 `src/vite-env.d.ts`，补齐 Vite 前端资源类型声明。
- 确认第一版 OCR 技术方向：优先采用 Tesseract.js，在浏览器本地识别中文和英文参数截图。
- 新增 IndexedDB 本地数据层，支持保存配方和样张 Blob。
- 新增 Tesseract.js OCR 服务入口，支持上传参数截图后识别为可编辑文本。
- 将 React 原型推进为可点击核心闭环：快查页、配方详情页、新建配方页、备份页。
- 新建页支持基础信息、品牌固定参数模板、效果样张、参数截图 OCR 文本、备注和保存。
- 快查页支持品牌 Tab、场景 / 题材 / 风格筛选、清空筛选和常用优先排序。
- 详情页支持样张封面、缩略图、参数分组展示和一键复制全部参数。
- 备份页支持导出 JSON 和导入 JSON 覆盖当前数据。
- 优化前端代码结构，将快查、详情、新建、备份拆成独立页面模块。
- 抽出表单类型、导航类型、配方工具函数、图片 URL hooks、ChipGroup 和 Field 业务组件。
- 将 `App.tsx` 收敛为应用状态编排和页面切换入口。
- 将富士「胶片模拟」参数从手动输入改为下拉选择。
- 新增富士胶片模拟选项数据，覆盖彩色胶片模拟、黑白和单色调。
- 将富士胶片模拟下拉从原生 select 改为自定义选择器，统一下拉框和选项样式。
- 优化选择器圆角、边距、字号、垂直居中和选中项主题色背景。

### 今日决策

- 全局样式入口优先使用相对路径，避免编辑器对「路径别名 + CSS 副作用导入」解析不稳定。
- OCR 第一版只做辅助识别文本，不强求自动归类字段。
- 效果样张保存到本机 IndexedDB；JSON 备份不内嵌图片文件。
- 页面级 UI 代码放入 `src/pages/`，跨页面复用的业务组件放入 `src/components/film-recipe/`。
- 富士「胶片模拟」字段使用固定选项，避免同一模拟名称出现多种手写格式。
- 需要精细控制下拉选项视觉时，不使用原生 option 样式，改用项目内自定义 Select 组件。

### 下一步

- 确认 React 高保真核心闭环的移动端视觉和交互。
- 开始补 OCR 文本到富士 / 理光参数字段的半自动匹配。

## 2026-07-30

### 今日完成

- 新增 UI 设计方向文档。
- 确定推荐视觉方向：Contact Sheet Notebook。
- 建立第一版颜色、字体、布局、组件和标志性视觉元素建议。
- 将 UI 设计方向链接加入 README 和产品设计文档。
- 将项目阶段从 MVP 原型确认推进到 UI 设计确认。
- 通过 Figma 插件新建 UI 初稿文件：[Film Recipe UI Draft](https://www.figma.com/design/b4cE1uGD9RrwC2Z7h4UYy1)。
- 生成 3 张可编辑 UI 初稿：快查页、配方详情页、新建配方页。
- 截图预览确认画面已生成且非空白。
- 根据用户反馈更新 Figma 初稿：底部导航改为浅色纸感，快查页新增明确品牌切换入口，新建页拆分效果样张和读取参数截图。
- 修正快查页品牌入口文字换行问题，并再次截图预览确认。
- 根据用户新一轮反馈继续打磨 Figma 初稿：快查页品牌入口由左上角卡片改为 Fuji / Ricoh Tab。
- 将快查页、配方详情页、新建配方页底部导航统一改为圆角、半透明、悬浮在底部的样式。
- 修正快查页筛选条右侧「清空」和筛选数量挤压问题，将清空筛选改为独立小胶囊按钮。
- 根据用户反馈收敛配方详情页：去掉「来源」和「预览参数截图」，详情页只保留最终参数。
- 新增 UI 规范文档，明确字体字号、行高、间距、按钮、卡片、导航栏圆角、阴影和边框规则。
- 确定组件库策略：采用 shadcn/ui 风格组件、Radix UI 和 Tailwind CSS，不使用 Ant Design 作为主组件库。
- 接入 Tailwind v4、`@` 路径别名和 shadcn 组件目录约定。
- 新增通用 UI 组件：Button、Card、Badge、Tabs。
- 新增 Film Recipe 业务组件：BottomNav、RecipeCard。
- 将首页改为基于组件的移动端快查原型壳。

### 今日决策

- 推荐产品气质为「私人摄影笔记 + 相机快查工具」。
- 推荐样张优先、控件克制、参数区接近相机菜单。
- 推荐使用 Contact Strip 作为功能性视觉元素。
- 推荐第一版高保真优先设计快查页、详情页、新建页。
- Figma 初稿后续可在同一文件中继续迭代。
- 整体气质保持「私人摄影笔记 + 相机快查工具」平衡。
- 视觉方向向文艺、手写摄影笔记感微调，不向强工具感收紧。
- 样张和参数截图是两类不同资产：样张用于判断配方效果，参数截图只用于 OCR 辅助录入。
- 快查页品牌切换采用 Tab，不使用左上角品牌卡。
- 底部导航采用半透明悬浮胶囊，保留「快查 / 新建 / 备份」三个入口。
- 来源是否来自手动输入或 OCR 不作为详情页信息。
- 开源组件基础选择 shadcn/Radix/Tailwind，是为了保留可复用性和视觉定制空间。

### 下一步

- 和用户确认 Figma 初稿的整体气质、配色、信息密度和页面结构。
- 初稿确认后，开始把 UI 方向实现到 React 应用。

## 2026-07-29

### 今日完成

- 确认本地 `main` 已和 `origin/main` 对齐，GitHub 同步完成。
- 运行 `npm run lint`，TypeScript 检查通过。
- 运行 `npm run build`，生产构建通过。
- 创建本地初始提交：`Initial Film Recipe project`。
- 确认远程仓库地址：`https://github.com/DecemberYvetteTang/film-recipe.git`。
- 新增第一版低保真原型文档，覆盖品牌入口、快查页、详情页、新建 / 编辑页和备份页。
- 根据用户反馈收敛第一版原型决策，并同步更新产品文档、TODO 和类型定义。

### 今日决策

- 第一版保留 3 个底部入口：快查、新建、备份。
- 新建 / 编辑页采用分段式单页，避免强迫用户完成完整向导。
- 配方卡片只展示样张、名称、状态、少量标签和样张来源，不展示完整参数。
- 配方详情页隐藏空参数字段，可疑字段标记为待确认。
- 第一版默认进入品牌入口页，不记忆上一次品牌。
- 快查页风格标签默认折叠，仅保留一个可见入口。
- 配方详情页需要一键复制全部参数。
- 新建配方保存后回到快查页。
- JSON 导入采用覆盖当前数据，不做合并。
- 第一版不加入模拟数据。
- 配方状态只保留「常用 / 想试」。
- 快查页需要清空筛选按钮。
- 快查页需要常用优先排序。
- 样张展示采用封面 + 缩略图，不做轮播。
- 详情页不展示来源模块和参数截图预览入口。
- 新建页默认从基础信息开始。
- 富士和理光参数字段需要分组。
- 图片不直接放进 JSON 备份。
- 图片字段改为通过 `assetId` 引用，避免备份 JSON 内嵌图片数据。

### 下一步

- 确认 UI 视觉方向后进入高保真设计稿。

## 2026-07-06

### 今日完成

- 确认项目名称为 Film Recipe。
- 确认第一版定位：手机优先、本地存储、以样张快查为核心的相机直出配方管理工具。
- 创建独立项目目录 `film-recipe`。
- 初始化 React + TypeScript + Vite 项目骨架。
- 建立核心领域类型：品牌、配方、富士参数、理光参数、样张、OCR 文本。
- 建立标签体系常量：场景、题材、风格、品牌机型。
- 建立产品设计文档、每日工作记录和 TODO 文件。

### 今日决策

- 第一版先做个人本地工具，不做登录、云同步和社区。
- 首页默认先选品牌。
- 配方列表使用样张瀑布流。
- 配方详情页使用相机菜单列表样式。
- OCR 作为辅助录入能力，不要求自动归类 100% 准确。
- 参数截图用于 OCR 辅助录入，不作为详情页来源证据。
- 本地数据需要支持 JSON 导入 / 导出备份。

### 明日建议

- 确认第一版页面流程和低保真原型。
- 确认新增配方表单字段优先级。
- 确认本地存储策略：localStorage、IndexedDB，或先 localStorage 后升级。
